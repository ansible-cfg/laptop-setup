setup_dir: "{{ansible_env.HOME}}/setup"
downloads_dir: "{{setup_dir}}/downloads"
gitlab_user: brpaz
gitlab_password: "{{ lookup('env','GITLAB_PASSWORD') }}"

# ===============================
# System variables
# ==============================
user: bruno
group: bruno
applications_dir: "{{ ansible_env.HOME}}/.local/share/applications"
icons_dir: "{{ ansible_env.HOME}}/.local/share/icons"
bin_dir: "/usr/local/bin"

# ===============================
# Desktop configuration
# ==============================
gtk_theme: Materia-light
icon_theme: Papirus-Adapta-Nokto
shell_theme: Materia-light

favorite_apps: ['google-chrome.desktop', 'org.gnome.Nautilus.desktop', 'com.gexperts.Tilix.desktop', 'spotify.desktop', 'boostnote.desktop', 'simplenote.desktop', 'franz.desktop', 'code.desktop', 'jetbrains-toolbox.desktop', 'mailspring.desktop','pinta.desktop','zeal.desktop']